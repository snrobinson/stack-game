<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleDisplayName</key>
	<string>Stack</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>

	<!-- Portrait only. The tower composition assumes a tall frame. -->
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
	</array>

	<key>UILaunchScreen</key>
	<dict>
		<key>UIColorName</key>
		<string>LaunchBackground</string>
	</dict>

	<key>UIRequiredDeviceCapabilities</key>
	<array>
		<string>armv7</string>
		<string>metal</string>
	</array>

	<key>UIStatusBarHidden</key>
	<true/>
	<key>UIViewControllerBasedStatusBarAppearance</key>
	<false/>
	<key>UIRequiresFullScreen</key>
	<true/>

	<!-- Shown in the ATT prompt. Kept plain and honest; vague copy here is a
	     common reason for a rejection. -->
	<key>NSUserTrackingUsageDescription</key>
	<string>Allow tracking so the ads you see in Stack can be more relevant. The game plays exactly the same either way.</string>

	<!-- Declares the app uses no non-exempt encryption, so App Store Connect
	     stops asking on every single upload. -->
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>

	<!-- SKAdNetwork identifiers go here when an ad SDK is linked. AdMob
	     publishes a current list; a stale or empty list silently costs
	     attribution revenue. -->
	<key>SKAdNetworkItems</key>
	<array/>
</dict>
</plist>
